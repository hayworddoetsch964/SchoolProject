function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

// Example usage:
let randomNumber = getRandomInt(100);
console.log(randomNumber); // Output: random integer between 0 and max - 1
